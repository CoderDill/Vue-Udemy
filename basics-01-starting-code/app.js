const app = Vue.createApp({
    data() {
        return {
            courseGoal: 'Finish course',
            vueLink: 'https://vuejs.org'
        };
    },
    methods: {
        
    }
});

app.mount("#user-goal")